import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.appdonations',
  appName: 'App Donations',
  webDir: 'www'
};

export default config;
